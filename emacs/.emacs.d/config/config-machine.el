;;; config-machine.el --- Machine specific configuration  -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(set-frame-font "Iosevka-16" nil t)
(setq-default custom-file "~/.emacscustom")

(provide 'config-machine)
;;; config-machine.el ends here
